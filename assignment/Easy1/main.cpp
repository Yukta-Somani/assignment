#include <bits/stdc++.h>
using namespace std;


int main()
{
    string str;
    getline(cin ,str);
    stringstream ss(str);
    string temp_word;
    int len_of_last_word=0;
    while(ss>>temp){
        len=temp.length();
    }
    cout << len;
    return 0;
}
